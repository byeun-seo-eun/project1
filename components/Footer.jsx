import styles from './footer.module.css'

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <div className="inner">
                <h2>footer</h2>
            </div>
        </footer>
    );
};

export default Footer;